"use client";

import { Dialog, DialogContent } from "@/components/ui/dialog";

export default function LoginModal() {
  return (
    <Dialog>
      <DialogContent>로그인 모달</DialogContent>
    </Dialog>
  );
}
